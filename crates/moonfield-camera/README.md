# Moonfield Camera

Scene-facing camera components and projection math for Moonfield.

The crate is independent of the Vulkan RHI. Render backends and render features
consume its camera data without exposing GPU types to scene code.

Part of the [moonfield](https://github.com/H5uan/moonfield) engine.
