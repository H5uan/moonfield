# Lunar Mare

Moonfield's Vulkan RHI (ash-based): GPU resources, command recording, shaders, and headless recording. The engine layer above it — extraction, view targets, the window frame loop, `RenderPlugin` — lives in the Selene crate (`moonfield-render-core`).

Part of the [moonfield](https://github.com/H5uan/moonfield) engine.
