version: 2
updates:
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "chore"
      include: "scope"
    labels:
      - "dependencies"
    groups:
      # swc crates are released in lockstep and must move together —
      # bumping one alone leaves two swc_common versions in the tree
      # and breaks the build (cf. the swc_ecma_ast 26 incident).
      swc:
        patterns:
          - "swc*"
      # The egui stack is version-locked and must move together —
      # egui_dock pins the egui/egui-winit pair (cf. the split-bump
      # failures superseded by PR #38).
      egui:
        patterns:
          - "egui*"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "chore"
      include: "scope"
    labels:
      - "dependencies"